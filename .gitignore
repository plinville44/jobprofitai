node_modules/
.next/
out/

# TypeScript's incremental build cache. Machine-specific, regenerated by
# every `tsc` run, and useless to anyone else - it produces a large noisy
# diff on every commit and conflicts on every merge.
*.tsbuildinfo

.env
.env.local
.DS_Store
*.log
npm-debug.log*
.vercel
